{
  "name": "sonniesedge",
  "version": "1.0.0",
  "private": true,
  "description": "sonniesedge",
  "scripts": {
    "hugo": "gulp hugo",
    "build": "gulp build",
    "build-preview": "gulp build-preview",
    "start": "gulp server",
    "lint": "eslint src"
  },
  "devDependencies": {},
  "dependencies": {
    "metalsmith": "^2.3.0",
    "metalsmith-markdown": "^0.2.2"
  }
}
